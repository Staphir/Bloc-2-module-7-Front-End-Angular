import { HighlightBackgroundColor } from './highlight-background-color';

describe('HighlightBackgroundColor', () => {
  it('should create an instance', () => {
    const directive = new HighlightBackgroundColor();
    expect(directive).toBeTruthy();
  });
});
